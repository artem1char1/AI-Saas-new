# Default: same-origin /api (nginx proxies to backend in docker-compose).
VITE_API_URL=/api

# If frontend and backend are on different public ports without nginx proxy, set:
# VITE_API_URL=http://YOUR_SERVER_IP:8000/api
# and add the frontend origin to backend CORS_ORIGINS.
