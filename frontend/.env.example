# Baked into the frontend bundle at docker build / yarn build time.
# Not read by the running nginx container.

# Same-origin API via nginx proxy to backend:8000 (recommended for docker-compose):
VITE_API_URL=/api

# Direct API from the browser (requires CORS on backend):
# VITE_API_URL=http://YOUR_SERVER_IP:8000/api
